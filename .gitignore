# Project-local files, never committed to git
.gitignore
AGENTS.md

# Node / TypeScript
node_modules/
dist/
*.tsbuildinfo

# Build artifact produced by scripts/build.mjs (source of truth lives in src/)
/monitor.ts

# Editor / OS
.DS_Store
*.swp

# Logs
*.log

# Parallel phase worktrees (swarm workflow, Round 1+)
.worktrees/

# Planning notes (local only, not part of swarm docs)
specs/

# Local install helper (personal use, never committed)
/scripts/install-local.sh

# Design/todo docs are local working notes, never committed to git
docs/
